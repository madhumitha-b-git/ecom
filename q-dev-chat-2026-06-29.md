Amazon Q can now help you write, modify, and maintain code by combining the power of natural language understanding with the ability to take actions on your behalf such as directly making code changes, modifying files, and running commands.

---

# Backend Refactoring Prompt

I have an existing FastAPI backend project written in Python using MongoDB. Currently, the entire backend is implemented in a monolithic structure where all Pydantic models are inside a single `models.py` file and all business logic is inside a single `services.py` file.

I want you to refactor the project into a clean, modular, production-ready architecture without changing the application&#39;s functionality.

## Requirements

### 1. Project Structure

Refactor the project into the following structure:

```text
backend/
│
├── app/
│   ├── api/
│   │   ├── routes/
│   │   │   ├── product_routes.py
│   │   │   ├── inventory_routes.py
│   │   │   ├── cart_routes.py
│   │   │   ├── payment_routes.py
│   │   │   └── order_routes.py
│   │
│   ├── models/
│   │   ├── product.py
│   │   ├── inventory.py
│   │   ├── cart.py
│   │   ├── payment.py
│   │   └── order.py
│   │
│   ├── services/
│   │   ├── product_service.py
│   │   ├── inventory_service.py
│   │   ├── cart_service.py
│   │   ├── payment_service.py
│   │   └── order_service.py
│   │
│   ├── database/
│   │   ├── mongodb.py
│   │   └── connection.py
│   │
│   ├── schemas/
│   │
│   ├── utils/
│   │
│   ├── config.py
│   ├── dependencies.py
│   ├── exceptions.py
│   ├── logger.py
│   └── main.py
│
├── tests/
│   ├── test_products.py
│   ├── test_inventory.py
│   ├── test_cart.py
│   ├── test_payment.py
│   ├── test_orders.py
│   └── conftest.py
│
├── .env
├── requirements.txt
├── pytest.ini
├── README.md
└── .gitignore
```

### 2. Models

Move every Pydantic model into its own file.

Example:

* product.py
* inventory.py
* cart.py
* payment.py
* order.py

Import them wherever necessary.

### 3. Service Layer

Split all business logic into separate service files.

Example:

Product CRUD logic should only exist inside product_service.py.

Inventory logic should only exist inside inventory_service.py.

Do not duplicate code.

### 4. API Routes

Create separate route files.

Each route file should expose only related endpoints.

Example:

product_routes.py

* GET /products
* GET /products/{id}
* POST /products
* PUT /products/{id}
* DELETE /products/{id}

Repeat similarly for inventory, cart, payment, and orders.

### 5. Database

Keep MongoDB connection in one place.

Use dependency injection where appropriate.

Avoid opening multiple MongoDB clients.

### 6. Error Handling

Create centralized exception handling.

Return meaningful HTTP status codes.

Handle:

* Invalid ObjectId
* Product Not Found
* Out of Stock
* Empty Cart
* Payment Failure
* Validation Errors

### 7. Logging

Create logger.py.

Log:

* API requests
* Errors
* Database operations
* Successful order creation
* Payment status

### 8. Configuration

Move all configuration values into .env.

Example:

MONGODB_URI

DATABASE_NAME

SECRET_KEY

DEBUG

Read them using Pydantic Settings or python-dotenv.

### 9. Code Quality

Improve readability by:

* Type hints everywhere
* Proper docstrings
* Remove duplicate code
* Follow PEP-8
* Use dependency injection
* Keep functions small and reusable

### 10. Unit Testing

Generate unit tests using pytest.

Create test cases for:

Products

* Create
* Read
* Update
* Delete
* Invalid ID

Inventory

* Update stock
* Out of stock

Cart

* Add item
* Remove item
* Empty cart

Orders

* Place order
* Cancel order

Payment

* Success
* Failure

Mock MongoDB where required.

### 11. API Documentation

Improve Swagger documentation.

For every endpoint:

* Summary
* Description
* Response models
* Status codes
* Example request
* Example response

### 12. README.md

Generate a professional README containing:

* Project Overview
* Features
* Tech Stack
* Architecture
* Folder Structure
* Installation
* Environment Variables
* Running the API
* API Documentation
* Testing
* Sample Requests
* MongoDB Collections
* Future Improvements

### 13. Do Not

Do not change the API behavior.

Do not change endpoint names unless absolutely necessary.

Keep the existing functionality working.

### 14. Goal

The final project should resemble a production-ready FastAPI backend following clean architecture principles and be suitable for internship evaluation and GitHub portfolio presentation.

---


Let me start by exploring your existing project structure to understand what we're working with.

---

I need permission to list directories outside the workspace.
`c:\Users\madhumitha.b\OneDrive - IDP Education Ltd\Desktop\ECOM`

---

You stopped your current work and asked me to work on the following task instead.

---

# Backend Refactoring Prompt

I have an existing FastAPI backend project written in Python using MongoDB. Currently, the entire backend is implemented in a monolithic structure where all Pydantic models are inside a single `models.py` file and all business logic is inside a single `services.py` file.

I want you to refactor the project into a clean, modular, production-ready architecture without changing the application&#39;s functionality.

## Requirements

### 1. Project Structure

Refactor the project into the following structure:

```text
backend/
│
├── app/
│   ├── api/
│   │   ├── routes/
│   │   │   ├── product_routes.py
│   │   │   ├── inventory_routes.py
│   │   │   ├── cart_routes.py
│   │   │   ├── payment_routes.py
│   │   │   └── order_routes.py
│   │
│   ├── models/
│   │   ├── product.py
│   │   ├── inventory.py
│   │   ├── cart.py
│   │   ├── payment.py
│   │   └── order.py
│   │
│   ├── services/
│   │   ├── product_service.py
│   │   ├── inventory_service.py
│   │   ├── cart_service.py
│   │   ├── payment_service.py
│   │   └── order_service.py
│   │
│   ├── database/
│   │   ├── mongodb.py
│   │   └── connection.py
│   │
│   ├── schemas/
│   │
│   ├── utils/
│   │
│   ├── config.py
│   ├── dependencies.py
│   ├── exceptions.py
│   ├── logger.py
│   └── main.py
│
├── tests/
│   ├── test_products.py
│   ├── test_inventory.py
│   ├── test_cart.py
│   ├── test_payment.py
│   ├── test_orders.py
│   └── conftest.py
│
├── .env
├── requirements.txt
├── pytest.ini
├── README.md
└── .gitignore
```

### 2. Models

Move every Pydantic model into its own file.

Example:

* product.py
* inventory.py
* cart.py
* payment.py
* order.py

Import them wherever necessary.

### 3. Service Layer

Split all business logic into separate service files.

Example:

Product CRUD logic should only exist inside product_service.py.

Inventory logic should only exist inside inventory_service.py.

Do not duplicate code.

### 4. API Routes

Create separate route files.

Each route file should expose only related endpoints.

Example:

product_routes.py

* GET /products
* GET /products/{id}
* POST /products
* PUT /products/{id}
* DELETE /products/{id}

Repeat similarly for inventory, cart, payment, and orders.

### 5. Database

Keep MongoDB connection in one place.

Use dependency injection where appropriate.

Avoid opening multiple MongoDB clients.

### 6. Error Handling

Create centralized exception handling.

Return meaningful HTTP status codes.

Handle:

* Invalid ObjectId
* Product Not Found
* Out of Stock
* Empty Cart
* Payment Failure
* Validation Errors

### 7. Logging

Create logger.py.

Log:

* API requests
* Errors
* Database operations
* Successful order creation
* Payment status

### 8. Configuration

Move all configuration values into .env.

Example:

MONGODB_URI

DATABASE_NAME

SECRET_KEY

DEBUG

Read them using Pydantic Settings or python-dotenv.

### 9. Code Quality

Improve readability by:

* Type hints everywhere
* Proper docstrings
* Remove duplicate code
* Follow PEP-8
* Use dependency injection
* Keep functions small and reusable

### 10. Unit Testing

Generate unit tests using pytest.

Create test cases for:

Products

* Create
* Read
* Update
* Delete
* Invalid ID

Inventory

* Update stock
* Out of stock

Cart

* Add item
* Remove item
* Empty cart

Orders

* Place order
* Cancel order

Payment

* Success
* Failure

Mock MongoDB where required.

### 11. API Documentation

Improve Swagger documentation.

For every endpoint:

* Summary
* Description
* Response models
* Status codes
* Example request
* Example response

### 12. README.md

Generate a professional README containing:

* Project Overview
* Features
* Tech Stack
* Architecture
* Folder Structure
* Installation
* Environment Variables
* Running the API
* API Documentation
* Testing
* Sample Requests
* MongoDB Collections
* Future Improvements

### 13. Do Not

Do not change the API behavior.

Do not change endpoint names unless absolutely necessary.

Keep the existing functionality working.

### 14. Goal

The final project should resemble a production-ready FastAPI backend following clean architecture principles and be suitable for internship evaluation and GitHub portfolio presentation.

---


Let me first explore your existing project to understand the current structure and code.

---

#### Allow read-only tools outside your workspace

---


I have a full picture of your existing code. Now I'll build the entire refactored structure. Let me create all directories and files systematically.

## Plan
1. Create all `__init__.py` files and directory structure
2. Config, logger, exceptions, dependencies
3. Database layer
4. Models (split per domain)
5. Services (split per domain)
6. Routes (split per domain)
7. main.py
8. Tests + conftest
9. requirements.txt, .env, pytest.ini, .gitignore, README.md

---

Creating the full directory structure for the refactored project

---

shell

---


Now create all `__init__.py` files:

---

Creating all __init__.py files to make packages importable

---

```shell
type nul > backend\app\__init__.py && type nul > backend\app\api\__init__.py && type nul > backend\app\api\routes\__init__.py && type nul > backend\app\models\__init__.py && type nul > backend\app\services\__init__.py && type nul > backend\app\database\__init__.py && type nul > backend\app\schemas\__init__.py && type nul > backend\app\utils\__init__.py && type nul > backend\tests\__init__.py